import { describe } from 'vitest';
import { screen } from '@testing-library/react';

describe('', () => {
  it('should ', () => {
    // eslint-disable-next-line no-restricted-globals,testing-library/no-debugging-utils
    screen.debug();
  });
});
